import fs from "fs"; import MT5Parser from "./lib/mt5Parser.js"; const html = fs.readFileSync("/app/public/mt5-real.html", "utf8"); const parser = new MT5Parser(html); const result = parser.parse(); console.log("=== ACCOUNT ==="); console.log(JSON.stringify(result.account, null, 2)); console.log("=== POSITIONS SUMMARY ==="); console.log(`Found ${result.positions.length} positions`); result.positions.forEach((p, i) => console.log(`${i+1}. ${p.symbol} ${p.type} P&L: ${p.profit}`)); console.log("=== RESULTS ==="); console.log(JSON.stringify(result.results, null, 2));
